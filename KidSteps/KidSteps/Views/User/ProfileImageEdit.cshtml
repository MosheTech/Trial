@model IEnumerable<KidSteps.Models.Image>
@{
    ViewBag.Title = "Edit";
}
<h2>
    Edit</h2>
<script src="@Links.Scripts.jquery_validate_min_js" type="text/javascript" />
<script src="@Links.Scripts.jquery_validate_unobtrusive_min_js" type="text/javascript" />
@using (Html.BeginForm())
{
    <input type="hidden" name="userId" value="@User.Identity.Name" />
    foreach (var imageId in Model)
    {
    @Html.RadioButtonFor(_ => imageId, imageId.Id)
    @Html.DisplayFor(_ => imageId)
    }
    <div>
        <input type="submit" value="Select" />
    </div>
}
<div>
    @Html.ActionLink("Upload new image", MVC.Image.Create(Request.RawUrl))
</div>
